# Geographic Disparities in Mental Health Care Access — California Counties

A reproducible county-level analysis of mental health care **availability** and **affordability** across California, using the Two Dimensions of Access dataset (OSF, 2024–2025).

## Key Findings

- Rural counties charge the **highest median session fees ($167.50)** despite having the lowest disposable incomes — creating a 4.9% ten-session burden vs. 3.8% in urban areas
- Urban Medicaid acceptance is **1.3%** — functionally zero — while micropolitan counties accept Medicaid at 10× that rate
- **Sierra County** carries the highest burden: nearly 10% of disposable income for ten sessions
- Access deserts cluster in the **northern interior and Central Valley**

## Data

This analysis uses the **Two Dimensions of Access** dataset (v1.0, January 2026), which draws on Psychology Today provider listings across U.S. counties.

**Download the data:** [https://osf.io/6ezrx](https://osf.io/6ezrx)

The CSV is **not included in this repository**. Place it in the `data/` folder before running:

```
data/
└── Two Dimension of access.csv
```

## Reproducing the Analysis

### Requirements

R (≥ 4.2) and Quarto (≥ 1.4). Install required packages once:

```r
install.packages(c(
  "tidyverse", "scales", "ggrepel", "tigris",
  "sf", "viridis", "patchwork", "gt", "plotly", "here"
))
```

### Run

```bash
quarto render index.qmd
```

Output renders to `docs/index.html`.

## Project Structure

```
mental-health-access-ca/
├── index.qmd          # Main Quarto notebook (analysis + narrative)
├── R/
│   └── analysis.R     # Standalone R script (same analysis, no narrative)
├── data/
│   └── README.md      # Data download instructions
├── figures/           # Exported static PNGs (generated by script)
├── _quarto.yml        # Quarto + GitHub Pages config
└── README.md          # This file
```

## Author

Pallavi Khushi · 2026  
Data source: Two Dimensions of Access (OSF) · Provider data: Psychology Today directory
